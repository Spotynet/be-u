import Settings from "@/app/settings";

/**
 * Settings screen for professionals/places (Mi Información).
 * Renders the main Settings component in embedded mode.
 */
export default function ProfileSettingsScreen() {
  return <Settings embedded />;
}
