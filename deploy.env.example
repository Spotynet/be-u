# Copy to deploy.env and fill in your values. deploy.env is gitignored.
#
# Required for backend deploy (scripts/deploy.sh):
EC2_PEM=~/pem/stg-be-u.pem
EC2_HOST=ec2-34-207-42-51.compute-1.amazonaws.com
#
# Optional (defaults shown):
EC2_USER=ubuntu
EC2_REPO_PATH=/home/ubuntu/be-u
