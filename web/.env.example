# Client-side API base (browser). Use nabbi dev API with nabbi_dev DB on the server.
NEXT_PUBLIC_API_URL=https://nabbi-api-dev.spotynet.com/api
# Next.js dev rewrites only — must match PM2 Django port (dev.sh uses 3007)
BACKEND_ORIGIN=http://127.0.0.1:3007
NEXT_PUBLIC_BACKEND_ORIGIN=http://127.0.0.1:3007
